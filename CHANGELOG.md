# Changelog

Все изменения в проекте URL Shortener.

## [1.1.0] - 2026-02-24

### 🐛 Исправленные баги

#### Критические
- **Nil pointer dereference в integration тестах** — передан `nil` logger в `NewRouter()`, что вызывало панику при каждом запросе
- **Отсутствие миграций БД в тестах** — таблицы не создавались перед запуском интеграционных тестов, все запросы падали с ошибкой `relation "links" does not exist`

#### Некритические
- **Пустой блок обработки ошибок кэширования** — ошибка игнорировалась без логгирования, теперь логируется через `logger.Warn()`
- **Недостаточно параметров в `NewLinkService`** — после добавления logger в сервис все вызовы требовали обновления
- **CI/CD: golangci-lint config** — неправильный формат `issues.exclude` (мапы вместо строк)
- **CI/CD: test flags** — конфликт флагов `-cover` и `-race`, интеграционные тесты требуют `-p 1`

### ✨ Улучшения

#### CI/CD
- Добавлен **GitHub Actions workflow** с этапами: Build, Test, Lint, Docker Build & Push
- Настроена публикация образов в **GitHub Container Registry (GHCR)**
- Автоматический запуск пайплайна при push в `main`/`develop` и создании PR
- Кэширование Go модулей и Docker слоёв для ускорения сборок

#### Код
- Добавлен `*zap.Logger` в `linkService` для централизованного логгирования
- Унифицировано создание сервисов во всех точках (main, тесты)
- Улучшена обработка ошибок в middleware и handlers

#### Тесты
- Добавлена функция `runMigrations()` для создания таблиц в тестовой БД
- Обновлены integration тесты с корректной инициализацией logger
- Все 21 юнит-тестов и 10 интеграционных тестов проходят успешно

#### Документация
- Добавлен раздел **CI/CD** в README с описанием пайплайна
- Добавлен бейдж статуса сборки
- Обновлён Makefile командой `lint`

#### Конфигурация
- Создан `.golangci.yml` с настройками для golangci-lint v2
- Отключены излишне строгие правила (package-comments, exported)
- Исключены тестовые файлы из некоторых проверок

### 📦 Зависимости

#### Добавлены
- `golangci/golangci-lint-action@v6` — линтер в CI
- `github.com/golang-migrate/migrate/v4` — миграции (для тестов)

#### Обновлены
- Все Go зависимости актуализированы через `go mod tidy`

### 📊 Покрытие тестами

| Пакет | Coverage |
|-------|----------|
| `internal/middleware` | 76.2% |
| `internal/service` | 51.8% |
| Интеграционные тесты | 10/10 ✅ |

### 🔧 Технические изменения

| Файл | Изменения |
|------|-----------|
| `.github/workflows/ci-cd.yml` | Новый CI/CD пайплайн |
| `.golangci.yml` | Конфигурация линтера |
| `internal/service/link_service.go` | Добавлен logger |
| `internal/service/link_service_test.go` | Обновлены тесты |
| `tests/integration_test.go` | Миграции + logger |
| `cmd/api/main.go` | Обновлён NewLinkService |
| `Makefile` | Команда `lint` |
| `README.md` | Раздел CI/CD |

---

## [1.0.0] - 2026-02-XX

### 🎉 Первый релиз

- Высокопроизводительный сервис сокращения URL
- Отслеживание кликов с Worker Pool
- Rate Limiting (Token Bucket)
- API Key аутентификация
- Кэширование через Redis
- Swagger документация
- Graceful Shutdown
